{
  "name": "imaginariumitch-backend",
  "version": "1.0.0",
  "description": "Low-latency game API backend for imaginariumitch Unity WebGL game",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "npx playwright test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.54.0",
    "@fastify/cors": "^11.2.0",
    "@sentry/node": "^8.55.0",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
